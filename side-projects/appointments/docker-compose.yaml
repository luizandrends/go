services:
  db:
    image: postgres:16
    restart: always
    ports:
      - 5432:5432
    volumes:
      - db:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: "docker"
      POSTGRES_DB: "appointments"
      POSTGRES_PASSWORD: "password"
volumes:
  db:
    driver: local